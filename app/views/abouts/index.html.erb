<div class="hero-unit">
	<h1>about<% if current_user %> - wiki<% end %></h1>
	<p><% if !current_user %>log in to <% end %>click on any of the sections to make an edit.</p>
</div>

<h3 class="heading">basics</h3>
<div class="row">
  <div class="span12 content round" style=""><br>
	<% @about_graphs.each do |about| %>
	  <div class="span4" style="margin-left: 0px; padding: 10px;">
	  <h3><%= best_in_place_if current_user, about, :title %></h3>
	  <p><%= best_in_place_if current_user, about, :content, type: :textarea %></p>
	  </div>
	<% end %>
    <br><br>
  </div>
</div>


<h3 class="heading">FAQ</h3>
  <div class="row">
  	<div class="span12 content round"><br>
   	<% @about_faqs.each do |about| %>
   	  <div class="span6" style="margin: 0px; padding: 10px 0px 10px 10px;">
  	    <h3><%= best_in_place_if current_user, about, :title %></h3>
  	    <p><%= best_in_place_if current_user, about, :content, type: :textarea %></p>
  	  </div>
  	<% end %>
    <br></div>
  </div>

<% if current_user %>
<h3 class="heading">create new section</h3>
<div class="well">
  <br>
  <%= simple_form_for @about do |f| %>
    <%= f.input :title %>
    <%= f.input :genre, :collection => ["paragraph", "FAQ"] %>
    <%= f.input :content %>
    <%= f.submit :style => "margin-left: 170px;", :class => "btn btn-primary" %>
  <% end %>
</div>
<% end %>